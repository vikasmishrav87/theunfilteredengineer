image: node:20

stages:
  - build
  - test

cache:
  paths:
    - node_modules/

build_job:
  stage: build
  script:
    - npm ci
    - npm run build
  artifacts:
    paths:
      - dist/
    expire_in: 1 week

test_job:
  stage: test
  script:
    - npm run build
  only:
    - main
